name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ secrets.DATABASE_URL }}"
      - run: echo "${{ vars.DEPLOY_ENV || 'staging' }}"
      - run: echo "${{ secrets.API_KEY }}"
